# Example reference

Place optional supporting material here.

Keep reference files focused and self-contained. The Agent should only need to read them when the current request benefits from the additional context.
